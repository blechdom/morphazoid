name: Verify and deploy Morphazoid to AWS

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false
      - name: Install locked dependencies
        run: npm ci
      - name: Verify application
        run: npm run verify
      - name: Build public artifact and component catalog
        run: npm run build:deploy
      - name: Verify the component catalog artifact
        run: |
          test -f dist/storybook/index.html
          test -f dist/storybook/iframe.html
          test -f dist/storybook/index.json
          npm run check:storybook-dist
      - uses: actions/upload-artifact@v7
        with:
          name: morphazoid-site
          path: dist
          if-no-files-found: error
          retention-days: 7

  deploy:
    if: >-
      github.ref == 'refs/heads/main' &&
      github.event_name != 'pull_request' &&
      vars.AWS_ACCOUNT_ID != '' &&
      vars.AWS_DEPLOY_ROLE_ARN != '' &&
      vars.AWS_SITE_BUCKET != '' &&
      vars.AWS_CLOUDFRONT_DISTRIBUTION_ID != ''
    needs: verify
    runs-on: ubuntu-latest
    concurrency:
      group: morphazoid-production
      cancel-in-progress: false
    environment:
      name: production
      url: https://morphazoid.com
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: morphazoid-site
          path: site
      - uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          aws-region: us-east-1
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          allowed-account-ids: ${{ vars.AWS_ACCOUNT_ID }}
          role-session-name: morphazoid-${{ github.run_id }}
      - name: Verify CloudFront is ready for Storybook
        run: |
          root_headers="$(mktemp)"
          storybook_headers="$(mktemp)"
          trap 'rm -f "$root_headers" "$storybook_headers"' EXIT

          curl --fail --silent --show-error --head \
            --retry 4 --retry-all-errors --retry-delay 2 \
            https://morphazoid.com/ >"$root_headers"

          # Do not use --fail for this request. The probe object is intentionally
          # absent before the first Storybook upload, but its CloudFront behavior
          # must still add the scoped response headers to the origin's 403 response.
          curl --silent --show-error --head \
            --retry 4 --retry-all-errors --retry-delay 2 \
            https://morphazoid.com/storybook/__cloudfront-policy-probe__ \
            >"$storybook_headers"

          grep -iE "^x-frame-options:[[:space:]]*DENY[[:space:]]*$" "$root_headers"
          grep -iE "^x-frame-options:[[:space:]]*SAMEORIGIN[[:space:]]*$" "$storybook_headers"
          grep -iE "^content-security-policy:[[:space:]]*frame-ancestors[[:space:]]+'self'[[:space:]]*$" \
            "$storybook_headers"
      - name: Publish static files
        run: |
          aws s3 sync site/ "s3://${{ vars.AWS_SITE_BUCKET }}/" \
            --delete \
            --only-show-errors \
            --cache-control "public,max-age=300,must-revalidate"
          aws s3 cp site/ "s3://${{ vars.AWS_SITE_BUCKET }}/" \
            --recursive \
            --exclude "*" \
            --include "*.html" \
            --only-show-errors \
            --cache-control "public,max-age=0,must-revalidate"
      - name: Invalidate CloudFront
        id: invalidation
        run: |
          invalidation_id="$(
            aws cloudfront create-invalidation \
              --distribution-id "${{ vars.AWS_CLOUDFRONT_DISTRIBUTION_ID }}" \
              --paths "/*" \
              --query "Invalidation.Id" \
              --output text
          )"
          echo "id=$invalidation_id" >> "$GITHUB_OUTPUT"
      - name: Wait for publication
        run: |
          aws cloudfront wait invalidation-completed \
            --distribution-id "${{ vars.AWS_CLOUDFRONT_DISTRIBUTION_ID }}" \
            --id "${{ steps.invalidation.outputs.id }}"
      - name: Smoke-test production
        run: |
          curl --fail --silent --show-error \
            --retry 8 --retry-all-errors --retry-delay 5 \
            https://morphazoid.com/ >/dev/null
          curl --fail --silent --show-error \
            --retry 8 --retry-all-errors --retry-delay 5 \
            https://morphazoid.com/morphazoidical/ >/dev/null
          curl --fail --silent --show-error \
            --retry 8 --retry-all-errors --retry-delay 5 \
            https://morphazoid.com/storybook/ >/dev/null
          curl --fail --silent --show-error \
            --retry 8 --retry-all-errors --retry-delay 5 \
            https://morphazoid.com/storybook/iframe.html >/dev/null
          curl --fail --silent --show-error \
            --retry 8 --retry-all-errors --retry-delay 5 \
            https://morphazoid.com/storybook/index.json >/dev/null
          curl --fail --silent --show-error --head \
            https://morphazoid.com/ \
            | grep -iE "^x-frame-options:[[:space:]]*DENY"
          curl --fail --silent --show-error --head \
            https://morphazoid.com/storybook/ \
            | grep -iE "^x-frame-options:[[:space:]]*SAMEORIGIN"
          curl --fail --silent --show-error --head \
            https://morphazoid.com/storybook/ \
            | grep -iE "^content-security-policy:.*frame-ancestors[[:space:]]+'self'"
          curl --fail --silent --show-error --head \
            https://morphazoid.com/src/contour-synth-processor.js \
            | grep -iE "^content-type:.*javascript"
