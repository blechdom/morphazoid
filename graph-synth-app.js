// Keep this query aligned with both Graph pages and the Graph Drums wrapper.
import { initializeGraphInstrument } from "./src/graph-instrument-app.js?v=graph-instruments-20260830-4";

initializeGraphInstrument({ mode: "synth" });
