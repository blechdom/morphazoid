# Shared Graph-page styles

`graph-instruments.css` was moved unchanged from the root. The Graph pages
reference it here; their controllers and their existing model/audio modules
remain separate.

This move does not merge Graph Synth, Graph Drums or Graph Delay into Graphs.
The owner wants the individual versions retained until the combined app is
satisfactory.
